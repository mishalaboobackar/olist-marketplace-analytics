# Secrets / credentials — NEVER commit
*.json
profiles.yml
.env
service-account*.json
keyfile*.json

# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/

# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# Data (too large / licensed — link in README instead)
data/
*.csv

# OS / editor / misc
.DS_Store
.Rhistory
.user.yml

# Private / personal scaffolds — keep local, not in the public repo
LEARNING_GUIDE.md
PROJECT_PLAN.md
